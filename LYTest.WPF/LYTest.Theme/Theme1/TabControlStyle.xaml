<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="TabItem">
        <Setter Property="Background" Value="{DynamicResource 窗口背景深色}"/>
        <Setter Property="Foreground" Value="{DynamicResource 字体颜色标准}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="_bordertop" Width="{TemplateBinding Width}" MinHeight="20" MaxWidth="{TemplateBinding MaxWidth}" Height="{TemplateBinding Height}" 
                            Background="{TemplateBinding Background}" 
                            BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" ToolTip="{TemplateBinding Header}" >
                        <Grid SnapsToDevicePixels="True">
                            <Label x:Name="_txt" VerticalAlignment="Center" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Content="{TemplateBinding Header}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter TargetName="_bordertop" Property="Background" Value="{DynamicResource 控件有效}" />
                            <Setter TargetName="_txt" Property="FontWeight" Value="Bold"/>
                            <Setter TargetName="_txt" Property="Foreground" Value="White"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="true"/>
                                <Condition Property="IsSelected" Value="false"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="_bordertop" Property="Background" Value="#33ca5100"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>
</ResourceDictionary>