<UserControl x:Class="LYTest.WPF.SG.Schema.View_AddSchema"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LYTest.WPF.SG.Schema" xmlns:ViewModel="clr-namespace:LYTest.ViewModel.Schema;assembly=LYTest.ViewModel" xmlns:Controls="clr-namespace:LYTest.WPF.SG.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ViewModel:SchemaOperationViewModel x:Key="SchemaModel"/>
    </UserControl.Resources>
    <Grid DataContext="{StaticResource SchemaModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border CornerRadius="5" Background="White" Margin="5">
            <ListBox Margin="3" ItemsSource="{Binding Schemas}" BorderThickness="0" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding SCHEMA_NAME}"
                                   ToolTip="{Binding SCHEMA_NAME}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <StackPanel Grid.Column="1"
                    Margin="3">
            <TextBlock Text="新方案名称"
                       Margin="3,10"/>
            <TextBox TextWrapping="WrapWithOverflow"
                     Text="{Binding NewName}"/>
            <Button Content="添加" Width="160"
                    Margin="5" Height="30"
                    Style="{DynamicResource SuccessButtonStyle}"
                    CommandParameter="AddSchema"
                    Command="{Binding LocalCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
