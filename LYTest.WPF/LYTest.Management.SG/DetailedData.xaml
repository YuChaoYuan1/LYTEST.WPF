<Window x:Class="LYTest.DataManager.SG.DetailedData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LYTest.DataManager.SG"
        mc:Ignorable="d"
        Title="详细数据" Height="600" Width="1200"
        Icon="数据管理.ico"
        WindowState="Maximized" Background="{DynamicResource 窗口背景色}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="340"/>
        </Grid.ColumnDefinitions>
        <Border CornerRadius="10" Margin="10,10,2,10" Background="White" >
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Margin="10">
                <StackPanel x:Name="pnlMeterResult"/>
            </ScrollViewer>
        </Border>

        <GridSplitter Grid.Column="1" Width="5" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

        <Border CornerRadius="10" Margin="3,10,10,10" Grid.Column="2" >
            <TabControl BorderBrush="{x:Null}" Margin="10" Background="Transparent">
                <TabItem Header="台体基本信息" Width="100">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="auto">
                            <StackPanel>
                                <StackPanel Name="stackPanelEquipInfo"/>
                                <StackPanel Name="stackPanelMark"/>
                            </StackPanel>
                        </ScrollViewer>
                        <Button Grid.Row="1"
                                    Height="30"
                                    Width="120"
                                    Click="Button_Click" 
                                    Margin="10"
                                Content="保存台体信息"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>

    </Grid>
</Window>
