<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="ExpandCollapseToggleStyle"  TargetType="{x:Type ToggleButton}">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Width"   Value="16"/>
        <Setter Property="Height"   Value="16"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Path x:Name="ExpandPath"
						  HorizontalAlignment="Center"
					      VerticalAlignment="Center"
                          Margin="1"
                           Data="M 2 0 L 10 7 L 2 14 z" 
					      Fill="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}">
                    </Path>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked"   Value="True"  >
                            <Setter TargetName="ExpandPath" Property="Fill" Value="Black"/>
                            <Setter TargetName="ExpandPath"    Property="LayoutTransform">
                                <Setter.Value>
                                    <TransformGroup>
                                        <ScaleTransform/>
                                        <SkewTransform/>
                                        <RotateTransform Angle="90"/>
                                        <TranslateTransform/>
                                    </TransformGroup>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate x:Key="CellTemplate_Name">
        <StackPanel Orientation="Horizontal" Margin="{Binding Level,Converter={StaticResource LevelToIndentConverter}}">
            <ToggleButton x:Name="Expander" 
                      Style="{StaticResource ExpandCollapseToggleStyle}" 
                      IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}"
                      HorizontalAlignment="Center" VerticalAlignment="Center" ClickMode="Press"/>
            <Image x:Name="imageCheck" Height="14" Margin="3,0" VerticalAlignment="Center"
                    Source="/Images/选中点.png" ToolTip="当前检定项"
					Visibility="{Binding IsCurrent, Converter={StaticResource BoolToVisibilityConverter}}"
                   UseLayoutRounding="True" SnapsToDevicePixels="True" />

            <CheckBox Margin="3,1" IsChecked="{Binding IsSelected,Mode=TwoWay}" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" VerticalAlignment="Center"  FontSize="{Binding NameFontSize}" >
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <Trigger  Property="FontSize" Value="14">
                                <Setter Property="FontWeight" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>



        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Path=HasItems, RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}" Value="False">
                <Setter TargetName="Expander"   Property="Visibility"  Value="Collapsed"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <GridViewColumnCollection x:Key="ColumnCollection">
        <GridViewColumn  Header="检定点列表"  Width="0" CellTemplate="{StaticResource CellTemplate_Name}" />
    </GridViewColumnCollection>

    <GridViewColumnCollection x:Key="ColumnCollectionFirst">
        <GridViewColumn  Header="检定点列表"  Width="240" CellTemplate="{StaticResource CellTemplate_Name}" />
        <GridViewColumn Width="40" Header="结论" >
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" ToolTip="{Binding DescNodeResult}" >
                        <Image  x:Name="imageResult" Source="/Images/不合格.png" Height="15" />
                        <TextBlock x:Name="textBlockFailCount" Foreground="Red" Margin="3,0,0,0" Text="{Binding FailCount}" />
                    </StackPanel>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding FailCount}" Value ="0">
                            <Setter TargetName="imageResult" Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding TestPass}" Value="true">
                            <Setter TargetName="imageResult" Property="Source" Value="/Images/合格.png"/>
                            <Setter TargetName="imageResult" Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding FailCount}" Value="0">
                            <Setter TargetName="textBlockFailCount" Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
    </GridViewColumnCollection>


    <!--明细-->
    <Style TargetType="TreeView" x:Key="TreeViewStyleResultSummary" >
        <Style.Resources>
            <Style TargetType="{x:Type TreeViewItem}">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="IsExpanded" Value="{Binding IsExpanded,Mode=TwoWay}"/>
                <Setter Property="Foreground" Value="{DynamicResource 字体颜色标准}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TreeViewItem}">
                            <StackPanel Background="{TemplateBinding Background}" Margin="0,1">
                                <Border Name="Bd" BorderThickness="0" Padding="0" VerticalAlignment="Center" >
                                    <GridViewRowPresenter x:Name="PART_Header" 
                                                          Content="{TemplateBinding Header}" 
                                                          Columns="{StaticResource ColumnCollection}" >

                                    </GridViewRowPresenter>
                                </Border>
                                <ItemsPresenter x:Name="ItemsHost" />
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsCurrent}" Value="true">
                                    <!--<Setter Property="IsSelected" Value="True" />-->
                                    <Setter Property="Background" TargetName="Bd" Value="{DynamicResource 网格选中项颜色}"/>
                                </DataTrigger>
                                <Trigger Property="IsExpanded"   Value="false">
                                    <Setter TargetName="ItemsHost"  Property="Visibility"  Value="Collapsed"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="HasHeader" Value="false"/>
                                        <Condition Property="Width" Value="Auto"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="PART_Header" Property="MinWidth" Value="75"/>
                                </MultiTrigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="HasHeader" Value="false"/>
                                        <Condition Property="Height"   Value="Auto"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="PART_Header" Property="MinHeight" Value="19"/>
                                </MultiTrigger>
                                <Trigger Property="IsSelected"  Value="true">
                                    <Setter Property="Background" TargetName="Bd" Value="{DynamicResource 网格选中项颜色}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled"  Value="false">
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="AlternationIndex" Value="1">
                        <Setter Property="Background" Value="{DynamicResource 表行颜色}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Style.Resources>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeView}">
                    <ScrollViewer >
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <DockPanel>
                                <GridViewHeaderRowPresenter Columns="{StaticResource ColumnCollection}" DockPanel.Dock="Top" Height="31" />
                                <ItemsPresenter/>
                            </DockPanel>
                        </Border>
                    </ScrollViewer>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="AlternationCount" Value="2"/>
    </Style>

    <!--树-->
    <Style TargetType="TreeView" x:Key="TreeViewStyleResultSummaryFirst" >
        <Style.Resources>
            <Style TargetType="{x:Type TreeViewItem}">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="IsExpanded" Value="{Binding IsExpanded,Mode=TwoWay}"/>
                <Setter Property="Foreground" Value="{DynamicResource 字体颜色标准}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TreeViewItem}">
                            <StackPanel Background="{TemplateBinding Background}"
                                        Margin="0,1">
                                <Border Name="Bd"
                                        BorderThickness="0" Padding="0"
                                        VerticalAlignment="Center" >
                                    <GridViewRowPresenter x:Name="PART_Header"
                                                          Content="{TemplateBinding Header}" 
                                                          Columns="{StaticResource ColumnCollectionFirst}" >

                                    </GridViewRowPresenter>
                                </Border>
                                <ItemsPresenter x:Name="ItemsHost" />
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsCurrent}" Value="true">
                                    <!--<Setter Property="IsSelected" Value="True" />-->
                                    <Setter Property="Background" TargetName="Bd" Value="{DynamicResource 网格选中项颜色}"/>
                                </DataTrigger>
                                <Trigger Property="IsExpanded"   Value="false">
                                    <Setter TargetName="ItemsHost"  Property="Visibility"  Value="Collapsed"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="HasHeader" Value="false"/>
                                        <Condition Property="Width" Value="Auto"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="PART_Header" Property="MinWidth" Value="75"/>
                                </MultiTrigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="HasHeader" Value="false"/>
                                        <Condition Property="Height" Value="Auto"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="PART_Header" Property="MinHeight" Value="19"/>
                                </MultiTrigger>
                                <Trigger Property="IsSelected"  Value="true">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource 网格选中项颜色}"/>
                                </Trigger>

                                <Trigger Property="IsEnabled"  Value="false">
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="AlternationIndex" Value="1">
                        <Setter Property="Background" Value="{DynamicResource 表行颜色}"/>
                    </Trigger>

                </Style.Triggers>
            </Style>
        </Style.Resources>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeView}">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden" >
                        <Border BorderBrush="{TemplateBinding BorderBrush}"  BorderThickness="{TemplateBinding BorderThickness}">
                            <DockPanel>
                                <GridViewHeaderRowPresenter  Columns="{StaticResource ColumnCollectionFirst}" DockPanel.Dock="Top" Height="31"/>
                                <ItemsPresenter/>
                            </DockPanel>
                        </Border>
                    </ScrollViewer>
                </ControlTemplate>
            </Setter.Value>
        </Setter>


        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="AlternationCount" Value="2"/>
    </Style>

    <Style TargetType="GridViewColumnHeader">
        <!--列名-->
        <Setter Property="Background" Value="{DynamicResource 网格列头}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource 边框颜色}"/>
        <Setter Property="Foreground" Value="{DynamicResource 字体颜色标准}"/>
    </Style>


</ResourceDictionary>