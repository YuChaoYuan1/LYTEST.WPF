<UserControl x:Class="LYTest.WPF.SG.Schema.View_ImportSchema"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border CornerRadius="5" Background="White" Margin="5">

            <ListBox Margin="5" BorderThickness="0"
                 SelectedItem="{Binding ImportSelectedSchema}"
                 ItemsSource="{Binding ImportSchemas}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding SCHEMA_NAME}" ToolTip="{Binding SCHEMA_NAME}"/>
                            <TextBlock >
                            <Run Text=" ("/>
                            <Run Text="{Binding SCHEMA_METER_TYPE}" ToolTip="{Binding SCHEMA_NAME}"/>
                            <Run Text="{Binding SCHEMA_ENABLE}" ToolTip="{Binding SCHEMA_NAME}"/>
                            <Run Text=")"/>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="180"/>
            </Grid.RowDefinitions>
            <GroupBox Header="项目列表">
                <ListBox ItemsSource="{Binding ImportItemList}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding PARA_NAME}" ToolTip="{Binding PARA_NAME}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>


            <StackPanel Grid.Row="1"
                    Margin="3">
                <TextBlock Text="方案名称:"
                       Margin="3,10"/>
                <TextBox Margin="3,0,3,10" Text="{Binding ImpostName}" TextWrapping="Wrap" 
                      Background="{DynamicResource 窗口背景深色}" />
                <Button Width="150" Height="40" Margin="3,10" Content="导入方案"
                    Style="{DynamicResource SuccessButtonStyle}"
                    CommandParameter="ImportSchema" Command="{Binding LocalCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
