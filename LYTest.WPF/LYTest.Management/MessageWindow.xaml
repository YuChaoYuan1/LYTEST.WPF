<Window x:Class="LYTest.DataManager.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LYTest.DataManager"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Title="Window_TipsMessage" Height="300" Width="1000"
        MouseMove="Window_MouseMove"
        FontSize="32"
        ResizeMode="NoResize" 
        Background="Transparent">
    <Border BorderThickness="1"  CornerRadius="10" Background="White" Margin="5">
        <Border.Effect>
            <DropShadowEffect Color="Gray"  ShadowDepth="0" BlurRadius="5" Opacity="0.3" Direction="0"></DropShadowEffect>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="10 10 0 0">
                <Border.Background>
                    <LinearGradientBrush  EndPoint="1,0.5" StartPoint="0,0.5">
                        <GradientStop Color="#257ACD"     Offset="0"/>
                        <GradientStop Color="#48B7FC"  Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid >
                    <TextBlock x:Name="TextMsg" Text="提示信息" 
                               VerticalAlignment="Center" HorizontalAlignment="Center" 
                               Foreground="White" TextWrapping="Wrap"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0">
                
                <Button  VerticalAlignment="Center" HorizontalAlignment="Center" 
                    Width="120" Height="50" Margin="0,0,0,0" Style="{Binding }"
                    Content="确定" Background="#48B7FC" Foreground="White" Click="Button_Click"  />
            </StackPanel>

        </Grid>
    </Border>
</Window>
