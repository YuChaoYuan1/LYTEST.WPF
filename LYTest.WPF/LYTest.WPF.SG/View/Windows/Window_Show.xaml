<Window x:Class="LYTest.WPF.SG.View.Windows.Window_Show"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Window_Show" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource 窗口背景色}"
        AllowsTransparency="True">
    <Window.Effect>
        <DropShadowEffect Color="Gray"  ShadowDepth="0" BlurRadius="5" Opacity="1" Direction="0.5"/>
    </Window.Effect>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Background="#003B77" MouseMove="UserControl_MouseMove">
            <StackPanel Orientation="Horizontal"  VerticalAlignment="Center" HorizontalAlignment="Left" >
                <Image x:Name="HeadIcon" Width="24" Height="24" Margin="5 0 5 0"/>
                <TextBlock Name="txt" VerticalAlignment="Center" Foreground="White" FontSize="14"/>
            </StackPanel>

            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="10,0">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="3,0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="MinWidth" Value="50" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="Button" x:Key="StyleBorderClose" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Gray"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Resources>
                <Button Click="Click_Max">
                    <Image Width="24" Name="imageMax" VerticalAlignment="Top"  Source="/Images/正常窗口.png" />
                </Button>
                <Button Click="Click_close">
                    <Image Width="24"  Name="imageClose"  VerticalAlignment="Top" Source="/Images/关闭.png"/>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Name="grid" Grid.Row="1">
        </Grid>
    </Grid>
</Window>
