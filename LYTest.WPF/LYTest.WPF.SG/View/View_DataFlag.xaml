<basemodel:DockControlDisposable  
             xmlns:basemodel="clr-namespace:LYTest.WPF.SG.Model" 
             x:Class="LYTest.WPF.SG.View.View_DataFlag"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LYTest.WPF.SG.View"
             mc:Ignorable="d" Margin="10"
             d:DesignHeight="450" d:DesignWidth="1200">
    <basemodel:DockControlDisposable.Resources>
        <XmlDataProvider x:Key="DataFlag" Source="/LYTest.WPF.SG;component/ConfigData/DataFlag.xml"/>
        <local:DataFlagItem x:Key="DataItemTemp"/>
    </basemodel:DockControlDisposable.Resources>
    <Grid DataContext="{Binding Source={StaticResource DataFlag}, XPath=/DataFlagInfo}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Margin="3" HeadersVisibility="Column" ItemsSource="{Binding XPath=/DataFlagInfo/R}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="标识名称" Width="200" Binding="{Binding XPath=@DataFlagDiName}"/>
                <DataGridTextColumn Header="数据标识" Width="80" Binding="{Binding XPath=@DataFlagDi}"/>
                <DataGridTextColumn Header="数据标识698" Width="80" Binding="{Binding XPath=@DataFlagOi}"/>
                <DataGridTextColumn Header="长度" Width="40" Binding="{Binding XPath=@DataLength}"/>
                <DataGridTextColumn Header="小数位" Width="50" Binding="{Binding XPath=@DataSmallNumber}"/>
                <DataGridTextColumn Header="格式" Width="120" Binding="{Binding XPath=@DataFormat}"/>
                <DataGridTextColumn Header="所属大类" Width="100" Binding="{Binding XPath=@ClassName}" />
                <DataGridTextColumn Header="安全模式" Binding="{Binding XPath=@EmSecurityMode}" Width="60"/>
                <DataGridTextColumn Header="读写权限" Binding="{Binding XPath=@Rights}" Width="60"/>
                <DataGridTextColumn Header="所属芯片" Binding="{Binding XPath=@Chip}" Width="60"/>
                <DataGridTextColumn Header="序号" Binding="{Binding XPath=@SortNo}" Width="40"/>
            </DataGrid.Columns>
        </DataGrid>
        <GridSplitter Grid.Column="1" Width="10"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Center" Background="Transparent"/>



        <Border  Grid.Column="2" Background="{DynamicResource 窗口背景色}" CornerRadius="10" Margin="0">
            <StackPanel DataContext="{Binding  Source={StaticResource DataItemTemp}}" Margin="10">
                <TextBlock Text="标识名称" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding Name}"/>
                <TextBlock Text="数据标识" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding DataFlag}"/>
                <TextBlock Text="数据标识698" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding DataFlag698}"/>
                <TextBlock Text="长度" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding Length}"/>
                <TextBlock Text="小数位" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding DotLength}"/>
                <TextBlock Text="格式" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding DataFormat}"/>

                <TextBlock Text="所属大类" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding ClassName}"/>

                <TextBlock Text="安全模式" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding EmSecurityMode}"/>

                <TextBlock Text="读写权限" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding Rights}"/>

                <TextBlock Text="所属芯片" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding Chip}"/>
                <TextBlock Text="排序号" Margin="3,1,3,0"/>
                <TextBox HorizontalAlignment="Stretch" Margin="3 0 3 3" Text="{Binding SortNo}"/>
                <Button Content="添加标识" Click="Click_Add" Margin="5,10" Height="25" Width="80"  Style="{DynamicResource PrimaryButtonStyle}"/>

            </StackPanel>

        </Border>


        <Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Click_Save" ToolTip="保存" Grid.Column="2" Margin="20">
            <Image Width="32" Height="32" Source="{DynamicResource 保存}"/>
        </Button>

    </Grid>
</basemodel:DockControlDisposable> 
